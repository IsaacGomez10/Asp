@model Asp.Models.producto
@{
    ViewBag.Title = "Cargar Imagen";
}

<h2>Cargar Imagen </h2>
<h3>Producto</h3>

@using (Html.BeginForm("CargarImagen", "ProductoImagen", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    @*<div class="form-group">
            <label class="control-label col-md-2" for="id_producto">Producto</label>
            <div class="col-md-10">
                @Html.Action("ListaProductos")
                @Html.ValidationMessageFor(model => model.producto_imagen, "", new { @class = "text-danger" })
                <br />
            </div>
        </div>*@
    <div class="form-group">
        <label class="control-label col-md-2">Producto</label>
        <div class="col-md-10">
            <input type="number" name="producto" class="form-control" />
        </div>
    </div>

    <div class="form-group">
        <label class="control-label col-md-2">Subir Imagen</label>
        <div class="col-md-10">
            <input type="file" name="imagen" />
            <br />
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-2">
            <input type="submit" value="Subir" class="btn btn-default" />

        </div>
    </div>

    @Html.ActionLink("Regresar a Lista", "PagIndex", "Producto")

}


